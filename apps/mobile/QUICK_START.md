# 🚀 Coffee Break Mobile App - Quick Start

## ✅ Setup Complete!

All dependencies are installed and configured. The app is ready to run!

## 🏃 Running the App

### 1. Make sure your backend API is running:
```bash
# In a separate terminal, from the project root:
cd apps/api
npm run dev
# API should be running on http://localhost:4000
```

### 2. Start the mobile app:
```bash
cd apps/mobile
npm start
```

### 3. Choose your platform:
- Press `i` - Open iOS Simulator
- Press `a` - Open Android Emulator  
- Press `w` - Open in web browser
- Scan QR code - Use Expo Go app on your phone

## 📱 What to Test

1. **Home Screen** - Should show coffee-themed hero section
2. **Coffees Tab** - Should fetch and display coffees from API
3. **Sellers Tab** - Should fetch and display sellers from API
4. **Scanner Tab** - Should request camera permission for QR scanning

## 🔧 Configuration

- **API URL**: Configured in `src/config/api.ts` (default: `http://localhost:4000`)
- **Theme**: Coffee-themed colors in `src/config/theme.ts`
- **Navigation**: Tab-based navigation with Expo Router

## 📝 Notes

- The app uses Expo SDK 54
- All dependencies are compatible and up-to-date
- Asset files (icons) will be auto-generated by Expo
- Camera permissions are configured in `app.json`

## 🐛 Troubleshooting

**If API calls fail:**
- Verify backend is running on port 4000
- Check CORS settings on API server
- Update API URL in `src/config/api.ts` if needed

**If camera doesn't work:**
- Grant permissions in device settings
- Check `app.json` has camera permissions configured

**If build fails:**
- Run `npm run clean:install`
- Clear Expo cache: `npx expo start --clear`

Enjoy your Coffee Break mobile app! ☕
