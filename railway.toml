[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
# Use Dockerfile CMD instead of overriding here
# The Dockerfile CMD runs ./scripts/start.sh which handles migrations and seeding
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
