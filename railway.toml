[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
startCommand = "sh -c 'cd apps/api && node dist/main.js'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
